# --- Core Frameworks ---
fastapi
uvicorn[standard]
flask
flask-cors

# --- Google OAuth2 Auth ---
google-auth
google-auth-oauthlib
requests
python-dotenv

# --- JWT Authentication ---
python-jose[cryptography]
passlib[bcrypt]  # untuk hashing password (kalau nanti kamu ingin tambah sistem login manual)

# --- Audio & Video Processing ---
openai-whisper
moviepy
ffmpeg-python
python-multipart

# --- Translation ---
# googletrans==4.0.0-rc1  # REMOVED - incompatible with Python 3.13 (requires deprecated 'cgi' module)
deep-translator  # Python 3.13 compatible alternative

# --- Database ---
sqlalchemy


